# SatQuery AI — local air-gapped orchestration.
# db: PostgreSQL 16 + PostGIS 3.4 (spatial GIS volume)
# backend: FastAPI on NVIDIA GPU, offline weight cache
# frontend: React compiled in-image, served on :3000

services:
  db:
    image: postgis/postgis:16-3.4
    container_name: satquery_db
    environment:
      - POSTGRES_USER=satquery_admin
      - POSTGRES_PASSWORD=isro_secure_db
      - POSTGRES_DB=satquery_gis
    ports:
      - "5432:5432"
    volumes:
      - satquery_db_data:/var/lib/postgresql/data
      - ./db/init-postgis.sh:/docker-entrypoint-initdb.d/init-postgis.sh
      - ./backend/app/database/init_db.sql:/docker-entrypoint-initdb.d/02-init_db.sql
    networks:
      - satquery_network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U satquery_admin -d satquery_gis"]
      interval: 10s
      timeout: 5s
      retries: 10
    restart: unless-stopped

  ollama:
    image: ollama/ollama:latest
    container_name: satquery_ollama
    ports:
      - "11434:11434"
    volumes:
      - satquery_ollama_data:/root/.ollama
    networks:
      - satquery_network
    restart: unless-stopped

  ollama-init:
    image: ollama/ollama:latest
    container_name: satquery_ollama_init
    depends_on:
      - ollama
    entrypoint: ["/bin/sh", "-c"]
    command: >
      "sleep 3;
       until (cat <<EOF | nc -w 1 ollama 11434 >/dev/null 2>&1
       GET /api/tags HTTP/1.1
       Host: ollama:11434

       EOF
       ); do echo 'Waiting for ollama service...'; sleep 3; done;
       echo 'Ollama is up. Checking models...';
       ollama pull llava || ollama pull llava-phi3 || true"
    networks:
      - satquery_network
    restart: "no"

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: satquery_backend
    
    ports:
      - "8000:8000"
    volumes:
      - ./backend:/app
      - ./backend:/app/backend
      - ./local_models:/app/models
      - ./local_models:/local_models:ro
      - ./data:/app/data
      - ./training:/app/training
      - ./scripts:/app/scripts
    environment:
      - DATABASE_URL=postgresql://satquery_admin:isro_secure_db@db:5432/satquery_gis
      - OLLAMA_HOST=http://ollama:11434
      - OLLAMA_BASE_URL=http://ollama:11434
      - LOCAL_MODELS_DIR=/local_models
      - PYTHONPATH=/app:/app/backend
      - HF_HUB_OFFLINE=1
      - TRANSFORMERS_OFFLINE=1
      - CORS_ORIGINS=http://localhost:3000,http://localhost:5173
    extra_hosts:
      - "host.docker.internal:host-gateway"
    depends_on:
      db:
        condition: service_healthy
      ollama:
        condition: service_started
    networks:
      - satquery_network
    healthcheck:
      test:
        [
          "CMD-SHELL",
          "python -c \"import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health')\"",
        ]
      interval: 20s
      timeout: 10s
      retries: 8
    restart: unless-stopped

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
      args:
        VITE_API_BASE_URL: ${VITE_API_BASE_URL:-http://localhost:8000}
    container_name: satquery_frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend
    networks:
      - satquery_network
    restart: unless-stopped

volumes:
  satquery_db_data:
  satquery_ollama_data:

networks:
  satquery_network:
    driver: bridge
